# Required: OpenAI API key with access to the Realtime API.
# Get one at https://platform.openai.com/api-keys
# The same key is used for the voice interview (Realtime API) and the post-interview grading (GPT-5.1).
OPENAI_API_KEY=
